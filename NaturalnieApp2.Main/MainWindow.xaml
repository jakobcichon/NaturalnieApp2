<Window x:Class="NaturalnieApp2.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NaturalnieApp2.Main" 
        xmlns:menubuttons="clr-namespace:NaturalnieApp2.SharedControls.MVVM.Views.MenuButtons;assembly=NaturalnieApp2.SharedControls"
        xmlns:menubuttonsviewmodels="clr-namespace:NaturalnieApp2.SharedControls.MVVM.ViewModels.MenuButtons;assembly=NaturalnieApp2.SharedControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="ListViewStyle" TargetType="ListView">
            
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <ListView Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Buttons}" Style="{StaticResource ListViewStyle}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <menubuttons:MenuButtonView DataContext="{Binding}"/>
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
